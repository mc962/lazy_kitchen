<turbo-frame id="<%= params.fetch(:turbo_frame, 'results') %>">
  <datalist id="autocomplete">
    <%= options_for_select((@results || []).map{|result| [result.name, result.name]}) %>
  </datalist>
</turbo-frame>
